#include <stdio.h>
struct date
{
    int days;
    int months;
    int years;
};
int main(int argc, char const *argv[])
{
    struct date d1, d2,diff;
    printf("Enter the first date in days ");
    scanf("%d", &d1.days);
    printf("Enter the first date in month ");
    scanf("%d", &d1.months);
    printf("Enter the first date in years ");
    scanf("%d", &d1.years);
    printf("Enter the second date in days ");
    scanf("%d", &d2.days);
    printf("Enter the second date in months ");
    scanf("%d", &d2.months);
    printf("Enter the second date in years ");
    scanf("%d", &d2.years);
    diff.days=d1
    return 0;
}
