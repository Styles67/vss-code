#include <stdio.h>
int main()
{
    int p, area, s;
    printf("Enter the side of the square you noob\n");
    scanf("%d", &s);
    p = 4 * s;
    printf("The perimeter of the square you noob is %d\n", p);
    area = s * s;
    printf("Area of the square you noob is %d\n", area);
    
    return 0;
}