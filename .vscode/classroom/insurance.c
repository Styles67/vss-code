#include <stdio.h>
int main()
{
    int Gender, age, area, health;
    printf("Enter the criteria of the person\n");
    scanf("%d", &Gender);
    printf("Enter the criteria of the person\n");
    scanf("%d", &age);
    printf("Enter the criteria of the person\n");
    scanf("%d", &area);
    printf("Enter the criteria of the person\n");
    scanf("%d", &health);
    if (Gender == "male")
    {
        if (age <= 25 && age >= 35)
        {
            if (area = city)
            {
                if (health == "excellent")
            }
            else
            {
                if (gender == "female")
            }
        }
    }